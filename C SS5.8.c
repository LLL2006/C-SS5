#include <stdio.h>

int main() {
    int n1, n2, bcnn;
    printf("Nhap hai so nguyen duong: ");
    scanf("%d %d", &n1, &n2);

     for(i=1;; i++) {
        if (i % a == 0 && i % b == 0) {
            bcnn = i;
            break;
        }

    printf("BCNN cua %d va %d la: %d\n", n1, n2, bcnn(n1, n2));
    return 0;
}

